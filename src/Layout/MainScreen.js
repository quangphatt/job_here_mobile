import React from 'react';
import { Text, View, Icon, Image, Button } from '@Components';

const MainScreen = () => {
  return (
    <View.Container>
    </View.Container>
  );
};

export default MainScreen;
